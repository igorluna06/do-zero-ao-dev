#include <stdio.h>

int main(void){

    /* Constante ela armazena um dado que não pode ser mais modificado após sua 
    declaração, diferente de uma variavel que é mutavel.*/

    const float pi = 3.14; //Declarando uma constante com const antes da declaração

    printf("%.2f \n", pi);

    return 0;

}