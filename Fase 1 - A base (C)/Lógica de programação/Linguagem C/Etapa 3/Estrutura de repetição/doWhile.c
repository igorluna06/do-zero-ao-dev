#include <stdio.h>

int main(void){

    int contador = 0;

    do
    {
        printf("Entrou");
        
    } while (contador < 0);

        /* Código comentado: O do while é uma estrutura de repetição que executa o bloco de código pelo menos uma vez antes de verificar a condição. 
        A sua estrutura é a seguinte:
        do {
            // bloco de código a ser executado
        } while (condição);

        */
    return 0;
}